@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model List<Movie>
<h1>Movies</h1>
<hr />

<a asp-controller="Movie" asp-action="CreateEditMovie" class="btn btn-primary">Create New</a>

<table class="table">
    <thead>
        <tr>

            <th>Id</th>
            <th>Value</th>
            <th>Description</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var expense in Model)
            {
                <tr>
                    <td>@expense.Id</td>
                    <td>@expense.Year</td>
                    <td>@expense.Title</td>
                    <td><a class="btn btn-primary" asp-action="CreateEditMovie" asp-route-id="@expense.Id">Edit</a></td>
                    <td><a class="btn btn-danger" asp-action="Delete" asp-route-id="@expense.Id">Delete</a></td>
                </tr>
            }

        }
    </tbody>
</table>